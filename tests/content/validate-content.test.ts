import{describe,it,expect}from"vitest";import a1 from"../../content/vocabulary/a1-core.json";import grammar from"../../content/grammar/a1-core.json";import reading from"../../content/reading-texts/a1/mein-tag.json";import listening from"../../content/listening/a1-core.json";describe("bilingual content integrity",()=>{it("every word has Arabic and contextual examples",()=>{for(const w of a1){expect(w.arabic).toBeTruthy();expect(w.exampleDe).toBeTruthy();expect(w.exampleAr).toBeTruthy()}});it("grammar has Arabic explanations and examples",()=>{for(const g of grammar){expect(g.explanationAr).toBeTruthy();expect(g.examples.length).toBeGreaterThan(0)}});it("reading has translation and comprehension",()=>{expect(reading.arabicTranslation).toBeTruthy();expect(reading.questions.length).toBeGreaterThan(0)});it("listening has transcripts",()=>{for(const x of listening){expect(x.transcript).toBeTruthy();expect(x.arabicTranslation).toBeTruthy()}})})