import{describe,it,expect}from"vitest";import{isSkillMastered,wordMastered,grammarMastered,canUnlockNextLevel}from"@/lib/engine/mastery-system";describe("mastery",()=>{it("uses the documented thresholds",()=>{expect(isSkillMastered(80)).toBe(true);expect(wordMastered(3)).toBe(true);expect(grammarMastered(5)).toBe(true)});it("requires every skill at 70 for level unlock",()=>{const good={vocabulary:70,grammar:70,listening:70,reading:70,writing:70,speaking:70,pronunciation:70,shadowing:70};expect(canUnlockNextLevel(good)).toBe(true);expect(canUnlockNextLevel({...good,listening:69})).toBe(false)})})