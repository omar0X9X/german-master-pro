import{describe,it,expect}from"vitest";import{lessonUnlocked,examUnlocked}from"@/lib/engine/unlock-manager";describe("unlock",()=>{it("opens first lesson",()=>expect(lessonUnlocked({id:"1",order:1})).toBe(true));it("requires previous completion and 70 score",()=>{expect(lessonUnlocked({id:"2",order:2,completedPrevious:true,scorePrevious:70})).toBe(true);expect(lessonUnlocked({id:"2",order:2,completedPrevious:true,scorePrevious:69})).toBe(false)});it("locks exam until level complete",()=>expect(examUnlocked(false)).toBe(false))})