export function Progress({value}:{value:number}){return <div className="h-2 rounded-full bg-white/10 overflow-hidden"><div className="h-full bg-[var(--brand)]" style={{width:`${Math.max(0,Math.min(100,value))}%`}}/></div>}
