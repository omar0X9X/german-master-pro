"use client"; import type { SkillMastery } from "@/types";
export function SkillsRadar({skills}:{skills:SkillMastery}){const entries=Object.entries(skills);const pts=entries.map(([_,v],i)=>{const a=(Math.PI*2*i)/entries.length-Math.PI/2,r=70*(v/100);return `${90+Math.cos(a)*r},${90+Math.sin(a)*r}`}).join(" ");return <div><svg viewBox="0 0 180 180" className="w-full max-w-[280px] mx-auto"><polygon points="90,15 143,37 165,90 143,143 90,165 37,143 15,90 37,37" fill="none" stroke="rgba(255,255,255,.15)"/><polygon points={pts} fill="rgba(217,255,79,.18)" stroke="#d9ff4f" strokeWidth="2"/></svg><div className="grid grid-cols-2 gap-1 text-xs muted">{entries.map(([k,v])=><span key={k}>{k}: {v}%</span>)}</div></div>}
