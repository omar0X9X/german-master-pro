"use client";import{useMemo}from"react";import{AdaptiveLearningEngine}from"@/lib/engine/adaptive-learning";import type{Profile,SkillMastery}from"@/types";export function useAdaptiveLearning(profile:Profile,skills:SkillMastery,due=0){const engine=useMemo(()=>new AdaptiveLearningEngine(),[]);return useMemo(()=>({weakest:engine.getWeakestSkill(skills),mastery:engine.calculateMastery(skills),plan:engine.getDailyPlan(profile,skills,due)}),[engine,profile,skills,due])}