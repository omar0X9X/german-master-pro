import{NextResponse}from"next/server";import{AIManager}from"@/lib/ai";export async function POST(req:Request){const{scenario="daily",level="A1"}=await req.json();return NextResponse.json({text:await new AIManager().generateConversation(scenario,level)})}
