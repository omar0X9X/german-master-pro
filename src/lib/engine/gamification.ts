export type Activity="lesson"|"review"|"speaking"|"writing"|"exam";const XP:Record<Activity,number>={lesson:40,review:5,speaking:30,writing:30,exam:100};export function awardXP(activity:Activity,streakDays=0){const multiplier=1+Math.min(streakDays,30)*.01;return Math.round(XP[activity]*multiplier)}export function levelFromXP(xp:number){return Math.max(1,Math.floor(Math.sqrt(Math.max(0,xp)/100))+1)}