import type{SkillMastery,SkillName}from"@/types";export function recommendContent(skills:SkillMastery,mistakes:Record<string,number>,interests:string[]=[]){const weak=(Object.entries(skills)as[SkillName,number][]).sort((a,b)=>a[1]-b[1])[0][0];const concept=Object.entries(mistakes).sort((a,b)=>b[1]-a[1])[0]?.[0];return{focusSkill:weak,repairConcept:concept??null,preferredTopic:interests[0]??"daily-life",reason:`أولوية ${weak} لأنها الأضعف حاليًا`}}