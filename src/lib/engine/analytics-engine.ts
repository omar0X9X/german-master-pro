export function learningVelocity(points:{date:string;mastery:number}[]){if(points.length<2)return 0;const sorted=[...points].sort((a,b)=>a.date.localeCompare(b.date));const days=Math.max(1,(Date.parse(sorted.at(-1)!.date)-Date.parse(sorted[0].date))/86400000);return Number(((sorted.at(-1)!.mastery-sorted[0].mastery)/days).toFixed(2))}export function predictedDaysToTarget(current:number,target:number,velocity:number){if(velocity<=0)return null;return Math.ceil(Math.max(0,target-current)/velocity)}